{
  "name": "budget-app",
  "version": "1.0.0",
  "description": "App per gestire il budget mensile",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build && mkdir -p ../backend/public && cp -r dist/* ../backend/public/",
    "start": "cd backend && NODE_ENV=production node server.js",
    "dev": "echo 'Run npm run dev in backend and frontend folders separately'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
